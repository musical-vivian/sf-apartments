[build]
builder = "nixpacks"

[deploy]
startCommand = "playwright install chromium --with-deps && uvicorn app.main:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/api/stats"
healthcheckTimeout = 60
restartPolicyType = "on_failure"
